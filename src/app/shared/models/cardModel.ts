export interface CardModel {
  id: number;
  cardName: string;
  cvv: string;
  cardNumber: string;
  cardValue: number;
  expirationDate: string;
  cardType: string;
  cardLimit: number;
  isActive: boolean;
}
